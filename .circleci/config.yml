version: 2.1
orbs:
  aws-s3: circleci/aws-s3@1.0.15

jobs:
  build-front:
    docker:
      - image: 'circleci/node:erbium'
    steps:
      - checkout
      - run: 
          name: Copy env and client build for staging
          command: |
            cd client && npm install && cp env-cmdrc .env-cmdrc && CI=false npm run build:staging

  deploy-front-s3:
    docker:
      - image: 'circleci/node:erbium'
    steps:
      - checkout
      - run: 
          name: Copy env and client build for staging
          command: |
            cd client && npm install && cp env-cmdrc .env-cmdrc && CI=false npm run build:staging
      - aws-s3/sync:
          arguments: |
            --acl public-read \
            --cache-control "max-age=86400"
          aws-access-key-id: AWS_ACCESS_KEY_ID
          aws-region: AWS_REGION
          aws-secret-access-key: AWS_SECRET_ACCESS_KEY
          from: client/build
          overwrite: true
          to: "s3://ms.codingtree.pl"


workflows:
  build-and-deploy-staging:
    jobs:
      - deploy-front-s3:
          filters:
            branches:
              only: circle-ci # only deploy on the circle-ci branch